# interactive-vb-program
A visual basic program to with simple features to start with

this simple program can add four values and load the result to a label
Also, the program can detect weather the result is a prime number or not.
Hence, provide a range inwhich the result is
